#!/usr/bin/node

class Rectangle {
  // Constructor with two parameters w, h
  constructor (w, h) {
    // Initialize instance attribute width and height
    this.width = w;
    this.height = h;
  }
}

module.exports = Rectangle;
