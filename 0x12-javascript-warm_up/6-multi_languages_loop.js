#!/usr/bin/node

// Array of the strings to print
const stringz = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Loops through each element of the array and prints it
for (let i = 0; i < stringz.length; i++) { console.log(stringz[i]); }
